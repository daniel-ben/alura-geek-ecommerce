import { StyledIconContainer } from "./styles"
import { SearchIcon } from "../SearchIcon"

export function SearchButton () {
  return(
    <StyledIconContainer>
      <SearchIcon />
    </StyledIconContainer>
  )
}