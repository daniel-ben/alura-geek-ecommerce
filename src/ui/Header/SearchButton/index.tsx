import { StyledIconContainer } from "./style"
import { SearchIcon } from "../SearchIcon"

export function SearchButton () {
  return(
    <StyledIconContainer>
      <SearchIcon />
    </StyledIconContainer>
  )
}